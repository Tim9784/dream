# 🌙 Интерпретация снов

Веб-приложение для интерпретации снов с использованием ChatGPT API.

## Как использовать

### Локальная разработка (Windows/Linux/Mac)

1. Установите зависимости: `pip install -r requirements.txt`
2. Запустите сервер: `python server.py`
3. Откройте `index.html` в браузере
4. При первом использовании введите ваш OpenAI API ключ (он сохранится в браузере)
5. Опишите ваш сон в текстовом поле
6. Нажмите "Интерпретировать сон"
7. Получите подробную интерпретацию вашего сна

### Запуск на Linux сервере (продакшен)

#### Быстрый запуск через скрипт

Самый простой способ - использовать готовый скрипт:
```bash
chmod +x start_server.sh
./start_server.sh
```

Скрипт автоматически определит, есть ли gunicorn, и запустит сервер оптимальным способом.

#### Вариант 1: Запуск через gunicorn (рекомендуется)

1. Установите зависимости: `pip install -r requirements.txt`
2. Запустите сервер:
   ```bash
   gunicorn -w 4 -b 0.0.0.0:5000 wsgi:app
   ```
   Где:
   - `-w 4` - количество воркеров (настройте под вашу систему)
   - `-b 0.0.0.0:5000` - адрес и порт (0.0.0.0 позволяет доступ извне)
   - `wsgi:app` - указывает на приложение в wsgi.py

3. Для запуска в фоне:
   ```bash
   nohup gunicorn -w 4 -b 0.0.0.0:5000 wsgi:app > server.log 2>&1 &
   ```

#### Вариант 2: Запуск через systemd (автозапуск)

1. Скопируйте пример файла и отредактируйте его:
   ```bash
   sudo cp dream-interpreter.service.example /etc/systemd/system/dream-interpreter.service
   sudo nano /etc/systemd/system/dream-interpreter.service
   ```
   
   Или создайте файл `/etc/systemd/system/dream-interpreter.service` вручную:
   ```ini
   [Unit]
   Description=Dream Interpreter Flask App
   After=network.target

   [Service]
   User=ваш_пользователь
   WorkingDirectory=/путь/к/timeclick
   Environment="PATH=/путь/к/venv/bin"
   ExecStart=/путь/к/venv/bin/gunicorn -w 4 -b 0.0.0.0:5000 wsgi:app
   Restart=always

   [Install]
   WantedBy=multi-user.target
   ```

2. Активируйте сервис:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable dream-interpreter
   sudo systemctl start dream-interpreter
   ```

3. Проверьте статус:
   ```bash
   sudo systemctl status dream-interpreter
   ```

#### Вариант 3: Прямой запуск Flask (для тестирования)

```bash
export HOST=0.0.0.0
export PORT=5000
export DEBUG=False
python server.py
```

#### Настройка переменных окружения

Вы можете настроить сервер через переменные окружения:
- `HOST` - хост для прослушивания (по умолчанию: 0.0.0.0)
- `PORT` - порт (по умолчанию: 5000)
- `DEBUG` - режим отладки (по умолчанию: False)

Пример:
```bash
export HOST=0.0.0.0
export PORT=8080
python server.py
```

#### Настройка веб-сервера (nginx)

Для продакшена рекомендуется использовать nginx как reverse proxy:

```nginx
server {
    listen 80;
    server_name ваш_домен.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static {
        alias /путь/к/timeclick;
    }
}
```

## Получение API ключа

1. Зарегистрируйтесь на [OpenAI](https://platform.openai.com/)
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Скопируйте ключ и вставьте при первом использовании приложения

## Файлы

- `index.html` - основная HTML страница
- `style.css` - стили для красивого дизайна
- `script.js` - логика обработки снов и отправка запросов
- `server.py` - Flask сервер для проксирования запросов к OpenAI API
- `wsgi.py` - WSGI файл для запуска через gunicorn
- `start_server.sh` - скрипт для удобного запуска на Linux
- `dream-interpreter.service.example` - пример systemd сервиса для автозапуска

## Особенности

- Преобразует описание сна в оптимизированный промпт для ChatGPT
- Использует GPT-3.5-turbo для анализа
- Красивый современный интерфейс
- Сохранение API ключа в браузере
- Адаптивный дизайн для мобильных устройств

## Загрузка на GitHub

📦 Подробная инструкция по загрузке проекта на GitHub находится в файле [GITHUB_SETUP.md](GITHUB_SETUP.md)

Краткая версия:
1. Установите Git: https://git-scm.com/download/win
2. Создайте репозиторий на GitHub.com
3. Выполните в папке проекта:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/YOUR_USERNAME/timeclick.git
   git push -u origin main
   ```

## Безопасность

⚠️ **Важно**: API ключ хранится только в localStorage браузера. Для продакшена рекомендуется использовать серверную часть для безопасного хранения ключей.
