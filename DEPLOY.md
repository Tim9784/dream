# 🚀 Развертывание на Linux сервере

## Клонирование репозитория с GitHub

### Вариант 1: Через HTTPS (простой способ)

1. Подключитесь к серверу по SSH:
   ```bash
   ssh username@your-server-ip
   ```

2. Перейдите в нужную директорию (например, `/var/www` или `/home/username`):
   ```bash
   cd /var/www
   # или
   cd ~
   ```

3. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/Tim9784/timeclick.git
   ```

4. Перейдите в директорию проекта:
   ```bash
   cd timeclick
   ```

### Вариант 2: Через SSH (если настроен SSH ключ)

```bash
git clone git@github.com:Tim9784/timeclick.git
```

## Установка зависимостей

1. Создайте виртуальное окружение (рекомендуется):
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

3. Если у вас Python 3.5 или ниже, и возникли проблемы с gunicorn:
   ```bash
   # Установите совместимую версию gunicorn
   pip install 'gunicorn==19.10.0'
   ```

## Обновление кода на сервере

После того, как вы внесли изменения в репозиторий на GitHub:

1. Перейдите в директорию проекта на сервере:
   ```bash
   cd /var/www/timeclick
   # или путь, куда вы клонировали репозиторий
   ```

2. Обновите код:
   ```bash
   git pull origin main
   ```
   
   Или если ветка называется `master`:
   ```bash
   git pull origin master
   ```

3. Если используете виртуальное окружение, активируйте его:
   ```bash
   source venv/bin/activate
   ```

4. Обновите зависимости (если requirements.txt изменился):
   ```bash
   pip install -r requirements.txt
   ```

5. Перезапустите сервер (если используете systemd):
   ```bash
   sudo systemctl restart dream-interpreter
   ```

## Автоматическое обновление через webhook (опционально)

Можно настроить автоматическое обновление при push в GitHub через webhook:

1. Создайте скрипт `deploy.sh`:
   ```bash
   #!/bin/bash
   cd /var/www/timeclick
   git pull origin main
   source venv/bin/activate
   pip install -r requirements.txt
   sudo systemctl restart dream-interpreter
   ```

2. Сделайте его исполняемым:
   ```bash
   chmod +x deploy.sh
   ```

3. Настройте webhook на GitHub (Settings → Webhooks → Add webhook)

## Полная установка с нуля

```bash
# 1. Клонирование репозитория
cd /var/www
git clone https://github.com/Tim9784/timeclick.git
cd timeclick

# 2. Создание виртуального окружения
python3 -m venv venv
source venv/bin/activate

# 3. Установка зависимостей
pip install -r requirements.txt

# 4. Настройка переменных окружения (если нужно)
export HOST=0.0.0.0
export PORT=5000
export DEBUG=False

# 5. Убедитесь, что gunicorn установлен в venv:
pip install gunicorn

# 6. Запуск сервера
# Через gunicorn напрямую (рекомендуется):
gunicorn -w 4 -b 0.0.0.0:5000 wsgi:app

# Или через скрипт:
chmod +x start_server.sh
./start_server.sh

# Или через bash:
bash start_server.sh
```

## Настройка systemd для автозапуска

1. Скопируйте и отредактируйте файл сервиса:
   ```bash
   sudo cp dream-interpreter.service.example /etc/systemd/system/dream-interpreter.service
   sudo nano /etc/systemd/system/dream-interpreter.service
   ```

2. Измените пути в файле:
   - `User=your_username` → ваш пользователь
   - `WorkingDirectory=/var/www/timeclick` → путь к проекту
   - `ExecStart=/var/www/timeclick/venv/bin/gunicorn` → путь к gunicorn

3. Активируйте сервис:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable dream-interpreter
   sudo systemctl start dream-interpreter
   ```

4. Проверьте статус:
   ```bash
   sudo systemctl status dream-interpreter
   ```

## Полезные команды

```bash
# Проверка статуса git
git status

# Просмотр истории коммитов
git log --oneline

# Откат к предыдущей версии (если что-то сломалось)
git reset --hard HEAD~1
git pull origin main

# Просмотр изменений перед обновлением
git fetch
git diff HEAD origin/main

# Создание резервной копии перед обновлением
cp -r /var/www/timeclick /var/www/timeclick_backup
```

## Решение проблем

### Ошибка "gunicorn: not found"

Если при запуске скрипта появляется ошибка `gunicorn: not found`:

1. Убедитесь, что виртуальное окружение активировано:
   ```bash
   source venv/bin/activate
   ```

2. Установите gunicorn в виртуальное окружение:
   ```bash
   pip install gunicorn
   ```

3. Запустите gunicorn напрямую:
   ```bash
   gunicorn -w 4 -b 0.0.0.0:5000 wsgi:app
   ```

4. Или используйте полный путь:
   ```bash
   venv/bin/gunicorn -w 4 -b 0.0.0.0:5000 wsgi:app
   ```

### Ошибка "start_server.sh: command not found"

Используйте правильный синтаксис:
```bash
# Вариант 1: С точкой и слэшем
./start_server.sh

# Вариант 2: Через bash
bash start_server.sh

# Вариант 3: С полным путем
bash /полный/путь/к/start_server.sh
```

Убедитесь, что скрипт имеет права на выполнение:
```bash
chmod +x start_server.sh
```

### Ошибка "Permission denied"
```bash
# Убедитесь, что у пользователя есть права на директорию
sudo chown -R $USER:$USER /var/www/timeclick
```

### Ошибка "git: command not found"
```bash
# Установите git
sudo apt update
sudo apt install git
```

### Ошибка "python3: command not found"
```bash
# Установите Python 3
sudo apt update
sudo apt install python3 python3-pip python3-venv
```

### Ошибка при git pull (локальные изменения)
```bash
# Сохраните изменения
git stash
git pull origin main
git stash pop
```

### Ошибка "SyntaxError: invalid syntax" с f-строками или в библиотеке openai

Если вы видите ошибку `SyntaxError: invalid syntax` на строке с `f"""` или `f"`, или ошибку в библиотеке openai (например, `**params,`), это означает, что на сервере Python 3.5, который не поддерживает современный синтаксис.

**Решение:** Код уже обновлен для совместимости с Python 3.5:
- Заменены f-строки на `.format()`
- Заменена библиотека `openai` на прямые HTTP-запросы через `requests`

Обновите код с GitHub и переустановите зависимости:
```bash
git pull origin main
pip install -r requirements.txt
```

**Важно:** Теперь используется библиотека `requests` вместо `openai`, что совместимо с Python 3.5.

### Ошибка "ImportError: No module named 'importlib.metadata'" или "NameError: name 'ModuleNotFoundError' is not defined"

Эта ошибка возникает, если на сервере установлен Python 3.5 или старше, а gunicorn требует Python 3.6+.

**Решение 1: Установите совместимую версию gunicorn для Python 3.5**
```bash
pip install 'gunicorn==19.10.0'
```

**Решение 2: Используйте Flask напрямую (для Python 3.5)**
```bash
# Установите переменные окружения
export HOST=0.0.0.0
export PORT=5000
export DEBUG=False

# Запустите через Flask
python server.py
```

**Решение 3: Обновите Python (рекомендуется для продакшена)**
```bash
# Проверьте текущую версию
python3 --version

# Если возможно, обновите Python до 3.6+
# Это может потребовать прав администратора
```

**Решение 4: Используйте waitress (альтернатива gunicorn для старых версий Python)**
```bash
pip install waitress
waitress-serve --host=0.0.0.0 --port=5000 wsgi:app
```

### Проверка версии Python
```bash
# Проверьте версию Python
python3 --version

# Если версия 3.5 или ниже, используйте одно из решений выше
```

### Проблема с кодировкой русского языка

Если русский текст отображается как кракозябры или знаки вопроса:

**Решение 1: Установите переменные окружения перед запуском**
```bash
export PYTHONIOENCODING=utf-8
export LC_ALL=en_US.UTF-8
export LANG=en_US.UTF-8

# Затем запустите сервер
gunicorn -w 4 -b 0.0.0.0:5000 wsgi:app
```

**Решение 2: Добавьте в systemd сервис (если используете)**
В файле `/etc/systemd/system/dream-interpreter.service` добавьте в секцию `[Service]`:
```ini
[Service]
Environment="PYTHONIOENCODING=utf-8"
Environment="LC_ALL=en_US.UTF-8"
Environment="LANG=en_US.UTF-8"
```

**Решение 3: Установите локаль на сервере**
```bash
# Проверьте доступные локали
locale -a

# Установите UTF-8 локаль (если доступна)
sudo locale-gen en_US.UTF-8
sudo update-locale LANG=en_US.UTF-8

# Или для русской локали
sudo locale-gen ru_RU.UTF-8
sudo update-locale LANG=ru_RU.UTF-8
```

**Решение 4: Код уже обновлен для автоматической настройки кодировки**
Просто обновите код с GitHub:
```bash
git pull origin main
```

Код автоматически настроит UTF-8 кодировку при запуске.

**Решение 5: Проверьте кодировку HTML файла на сервере**
```bash
# Проверьте кодировку файла
file -i index.html
# Должно показать: index.html: text/html; charset=utf-8

# Если кодировка неправильная, пересохраните файл в UTF-8
# Или используйте iconv для конвертации
iconv -f WINDOWS-1251 -t UTF-8 index.html > index.html.utf8
mv index.html.utf8 index.html
```

**Решение 6: Если используете nginx, добавьте в конфигурацию**
```nginx
server {
    charset utf-8;
    source_charset utf-8;
    
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # Важно для кодировки
        proxy_set_header Accept-Charset "utf-8";
    }
}
```

**Решение 7: Убедитесь, что файлы сохранены в UTF-8**
```bash
# Проверьте все HTML/JS файлы
file -i *.html *.js

# Если нужно, пересохраните файлы в UTF-8 через редактор
# Или используйте recode
recode WINDOWS-1251..UTF-8 index.html
```

